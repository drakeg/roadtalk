import { LocationLifecycleController } from "../location/LocationLifecycleController";

test("owner location snapshot exposes local coordinates for map rendering", () => {
  expect(LocationLifecycleController).toBeDefined();
});
